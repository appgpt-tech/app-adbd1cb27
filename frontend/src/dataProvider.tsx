//Source code generated by AppGPT (www.appgpt.tech)
import { GetListParams } from 'ra-core';
import postgrestRestProvider from '@promitheus/ra-data-postgrest';
import qs from 'qs';

const fieldConfig = {
  participants: [
    {
      field: 'participantId',
      datatype: 'Integer',
    },
    {
      field: 'fullName',
      datatype: 'String',
    },
    {
      field: 'email',
      datatype: 'String',
    },
    {
      field: 'phone',
      datatype: 'String',
    },
    {
      field: 'city',
      datatype: 'String',
    },
    {
      field: 'profilePicture',
      datatype: 'String',
    },
    {
      field: 'dateOfBirth',
      datatype: 'DateTime',
    },
    {
      field: 'gender',
      datatype: 'String',
    },
    {
      field: 'bio',
      datatype: 'String',
    },
    {
      field: 'skills',
      datatype: 'String',
    },
    {
      field: 'previousHackathons',
      datatype: 'Integer',
    },
    {
      field: 'schoolOrParty',
      datatype: 'String',
    },
    {
      field: 'awards',
      datatype: 'String',
    },
    {
      field: 'languages',
      datatype: 'String',
    },
    {
      field: 'interests',
      datatype: 'String',
    },
    {
      field: 'teamsSignedUpFor',
      datatype: 'Integer',
    },
    {
      field: 'posts',
      datatype: 'String',
    },
    {
      field: 'following',
      datatype: 'Integer',
    },
    {
      field: 'followers',
      datatype: 'Integer',
    },
    {
      field: 'directMessages',
      datatype: 'Integer',
    },
    {
      field: 'settings',
      datatype: 'String',
    },
    {
      field: 'specialties',
      datatype: 'String',
    },
    {
      field: 'socialMediaAccounts',
      datatype: 'String',
    },
    {
      field: 'createTeam',
      datatype: 'Integer',
    },
    {
      field: 'id',
      datatype: 'Integer',
    },
  ],
  sponsors: [
    {
      field: 'sponsorId',
      datatype: 'Integer',
    },
    {
      field: 'sponsorName',
      datatype: 'String',
    },
    {
      field: 'sponsorLogo',
      datatype: 'String',
    },
    {
      field: 'specialties',
      datatype: 'String',
    },
    {
      field: 'website',
      datatype: 'String',
    },
    {
      field: 'email',
      datatype: 'String',
    },
    {
      field: 'phoneNumber',
      datatype: 'String',
    },
    {
      field: 'address',
      datatype: 'String',
    },
    {
      field: 'posts',
      datatype: 'String',
    },
    {
      field: 'previousHackathons',
      datatype: 'Integer',
    },
    {
      field: 'followers',
      datatype: 'Integer',
    },
    {
      field: 'followings',
      datatype: 'Integer',
    },
    {
      field: 'newHackathon',
      datatype: 'Integer',
    },
    {
      field: 'id',
      datatype: 'Integer',
    },
  ],
  teams: [
    {
      field: 'leaderParticipant',
      datatype: 'Integer',
    },
    {
      field: 'participant',
      datatype: 'Integer',
    },
    {
      field: 'previousHackathons',
      datatype: 'Integer',
    },
    {
      field: 'awards',
      datatype: 'String',
    },
    {
      field: 'dateOfCreation',
      datatype: 'DateTime',
    },
    {
      field: 'id',
      datatype: 'Integer',
    },
  ],
  hackathons: [
    {
      field: 'sponsor',
      datatype: 'Integer',
    },
    {
      field: 'participantsOrTeams',
      datatype: 'Integer',
    },
    {
      field: 'maxAndMinParticipantsNumberForEachTeam',
      datatype: 'Integer',
    },
    {
      field: 'awards',
      datatype: 'String',
    },
    {
      field: 'bio',
      datatype: 'String',
    },
    {
      field: 'specialties',
      datatype: 'String',
    },
    {
      field: 'datesOfStages',
      datatype: 'DateTime',
    },
    {
      field: 'arbitrators',
      datatype: 'Integer',
    },
    {
      field: 'collaboratingSponsors',
      datatype: 'Integer',
    },
    {
      field: 'status',
      datatype: 'String',
    },
    {
      field: 'rankingOfParticipants',
      datatype: 'Integer',
    },
    {
      field: 'followers',
      datatype: 'Integer',
    },
    {
      field: 'id',
      datatype: 'Integer',
    },
  ],
};

export const customDataProvider = (apiUrl, options = {}) => {
  const baseDataProvider = postgrestRestProvider(apiUrl);
  return {
    ...baseDataProvider,
    getList: async (resource, params) => {
      if (params.filter && params.filter.q) {
        return await GetListQ(resource, params, apiUrl, baseDataProvider);
      } else {
        return baseDataProvider.getList(resource, params);
      }
    },
  };
};

async function GetListQ(
  resource,
  params: Partial<GetListParams> = {},
  apiUrl,
  baseDataProvider,
) {
  const { page, perPage } = params.pagination;
  const { field, order } = params.sort || {};
  const searchText = params.filter.q.toLowerCase();

  let filter = { or: '(id.eq.-1)' }; //return no data if resource has not been configured
  if (fieldConfig[resource]) {
    let searchConditions = fieldConfig[resource].reduce(
      (conditions, { field, datatype }) =>
        conditions +
        (datatype == 'String'
          ? field + '.ilike.%' + searchText + '%,'
          : //Todo: add support for searching other types
            ''),
      '',
    );
    filter = { or: '(' + searchConditions.slice(0, -1) + ')' };
  }
  let query = {
    offset: String((page - 1) * perPage),
    limit: String(perPage),
    // append filters
    ...filter,
  };

  if (field && order) {
    query.order = field + '.' + order.toLowerCase();
  }

  // add header that Content-Range is in returned header
  const options = {
    headers: new Headers({
      Accept: 'application/json',
      Prefer: 'count=exact',
      ...(params.meta?.headers || {}),
      //...useCustomSchema(config.schema, metaSchema, 'GET'),
    }),
  };

  const url = apiUrl + '/' + resource + '?' + qs.stringify(query);
  const response = await fetch(url, options);
  if (!response.ok) {
    const message = 'An error has occured: ' + response.status;
    throw new Error(message);
  }
  if (!response.headers.has('content-range')) {
    throw new Error(
      `The Content-Range header is missing in the HTTP Response. The postgREST data provider expects 
            responses for lists of resources to contain this header with the total number of results to build 
            the pagination. If you are using CORS, did you declare Content-Range in the Access-Control-Expose-Headers header?`,
    );
  }
  const json = await response.json();
  return {
    data: json,
    total: parseInt(response.headers.get('content-range').split('/').pop(), 10),
  };
}
